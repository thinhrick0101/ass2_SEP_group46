"""
This module provides a simple example function that prints a greeting.
"""
def divide(numerator, denominator):
    """
    This module provides a simple example function that prints a greeting.
    """
    if denominator == 0:
        raise ValueError("Cannot divide by zero")
    return numerator / denominator
